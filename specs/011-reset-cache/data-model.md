# Data Model

## Entities
- **AssetVersionToken**: query string added to `css/main.css` and `js/main.js`.
- **CachePolicy**: existing immutable headers for `/css/*` and `/js/*`.

## Notes
Only asset URLs change; headers remain unchanged.
