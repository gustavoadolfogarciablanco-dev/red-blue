# Data Model

## Entities
- **ContactPayload**: `{ name, email, message }` submitted to `/api/contact`.
- **HandlerExport**: CommonJS `module.exports` for the API function.

## Notes
No schema changes; payload and response remain unchanged.
