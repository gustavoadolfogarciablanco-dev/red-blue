# Contracts

Behavior contract for 010-fix-api-esm-warning.

## Behavior
- `/api/contact` uses CommonJS export (`module.exports`).
- Request payload remains `{ name, email, message }`.
- Response behavior is unchanged; only the build warning is removed.
