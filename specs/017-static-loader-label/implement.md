# Implement: Static loader label fallback

## Goal
Ensure the loader label never shows an i18n key.

## Steps
1. Remove i18n bindings from the loader markup.
2. Verify the loader shows a static label.
