# Implement: Force HTML revalidation for cache reset

## Goal
Ensure clients revalidate HTML and load the latest assets.

## Steps
1. Add revalidation headers for `/` and `/index.html`.
2. Deploy and verify.
