# Data Model

## Entities
- **LoaderState**: `app-loading` -> `app-ready` on i18n + load or timeout.
- **HtmlCachePolicy**: no-store headers for `/` and `/index.html`.
- **AssetVersionToken**: query string for CSS/JS.

## Notes
No persistent data is added.
